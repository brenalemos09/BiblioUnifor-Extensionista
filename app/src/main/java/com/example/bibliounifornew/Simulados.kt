package com.example.bibliounifornew

/**
 * Objeto para simular armazenamento de dados em memória (ArrayList simulada)
 */
object Simulados {
    // Lista de notificações para o usuário
    val notificacoesUsuario = ArrayList<String>()
    
    // Lista de registros financeiros (exemplo)
    val registrosFinanceiros = ArrayList<String>().apply {
        add("O Alienista")
    }
}